# Build information

build.file=${project.artifactId}-${project.version}.jar
build.timestamp=${timestamp}

# Maven information

maven.version=${project.version}
maven.groupId=${project.groupId}
maven.artifactId=${project.artifactId}

# Git information

git.branch=${git.branch}
git.repository.name=${git.repository.name}
git.sha=${git.sha}
git.version.long=${git.version.long}
